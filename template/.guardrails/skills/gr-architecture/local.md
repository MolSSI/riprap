# Local Extensions to the gr-architecture Skill

This agent-neutral file belongs to your project. The Guardrails template creates it once and never
updates it, so anything you write here survives `copier update`.

Instructions added below extend the gr-architecture skill. Where they conflict with the
skill's built-in instructions, this file wins.

Examples of instructions you might add:

<!--
- The architecture document lives at `docs/ARCHITECTURE.md` instead of `rqm/ARCHITECTURE.md`.
- Always include a "Deployment" section describing the target runtime environment.
- This project targets GPUs via CUDA; treat CPU-only execution as out of scope by default.
-->
