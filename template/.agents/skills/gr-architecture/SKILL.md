---
name: gr-architecture
description: Establish or revise the project's high-level goals and architecture before feature planning or implementation.
---

Read `.claude/skills/gr-architecture/SKILL.md` completely and follow it as the authoritative
workflow. It is shared with Claude Code so Guardrails has one maintained implementation of this
skill. When applying it in Codex:

- Treat `$gr-plan` and `$gr-implement` as the Codex equivalents of `/gr-plan` and `/gr-implement`.
- Read `AGENTS.md` for project guidance when the shared workflow refers to `CLAUDE.md`.
- Use Codex's available user-input mechanism when the workflow refers to `AskUserQuestion`.
